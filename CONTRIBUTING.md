# Contributing

This project is early. Contributions should preserve the core contract:

- keep runtime adapters separate from the workflow core,
- keep evidence and verification first-class,
- avoid committing private media, cookies, signed URLs, or model weights,
- add tests for schema and workflow behavior.

Open an issue before large changes so the task can be tracked in both GitHub
and the Paperclip control plane.

